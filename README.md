- This writes the markdown content to `README.md` using `>` to redirect output to the file.
- Run it exactly as shown to avoid quote issues.

3. **Verify the File:**
- Check the content:
  ```bash
  cat README.md
